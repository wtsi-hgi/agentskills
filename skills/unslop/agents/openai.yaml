interface:
  display_name: "Unslop"
  short_description: "Cleans prose and mechanics in Markdown files"
  default_prompt: "Use $unslop when writing or revising a Markdown file, to apply plain prose and the repository's Markdown mechanics."

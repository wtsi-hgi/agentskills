interface:
  display_name: "PR Reviewer"
  short_description: "Reviews and fixes the current branch"
  default_prompt: "Use $pr-reviewer to review this branch against its base for bugs, quality, and usability, then fix each finding via subagents."

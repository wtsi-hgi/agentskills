interface:
  display_name: "Final Response"
  short_description: "Cuts filler from every final chat response"
  default_prompt: "Use $final-response before sending the final answer, to keep only concrete, useful, and verified content."
